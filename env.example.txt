# --- База -------------------------------------------------------------------
# Postgres (Neon). Vercel-интеграция кладёт строку в POSTGRES_URL — её тоже
# читаем, отдельно копировать не нужно.
DATABASE_URI=postgres://user:password@host/dbname?sslmode=require

# Любая длинная случайная строка — подписывает сессии Payload
PAYLOAD_SECRET=change-me

# Публичный адрес сайта, нужен для canonical, og:url и sitemap
NEXT_PUBLIC_SITE_URL=https://future-arch.md

# --- Почта ------------------------------------------------------------------
# Без SMTP_HOST письма не отправляются, но заявки всё равно сохраняются
# в админке. Кому их слать — настраивается в админке: «Заявки — уведомления».
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=no-reply@lh47arch.com
SMTP_PASS=app-password
SMTP_FROM=no-reply@lh47arch.com
SMTP_FROM_NAME=Future Architecture

# Запасной адрес, если глобал «Заявки — уведомления» ещё не заполнен
APPLICATIONS_EMAIL=marketing-team@lh47arch.com

# --- Аналитика --------------------------------------------------------------
# Всё это удобнее задавать в админке («Аналитика и счётчики»).
# Переменные — запасной вариант; значение из админки имеет приоритет.
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_GA4_ID=
GOOGLE_SITE_VERIFICATION=
NEXT_PUBLIC_YANDEX_ID=
NEXT_PUBLIC_META_PIXEL_ID=
